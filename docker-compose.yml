services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    # args:
    #   user: arie
    #   uid: 1001
    container_name: serayu_php
    volumes:
      - ./:/var/www
    networks:
      - serayu

  nginx:
    image: nginx:stable-alpine
    container_name: serayu_nginx
    ports:
      - "8111:80"
    volumes:
      - ./:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php
    networks:
      - serayu

  redis:
    image: redis:alpine
    container_name: serayu_redis
    # ports:
    #   - "6380:6379"
    networks:
      - serayu

#   echo-server:
#     image: node:18-alpine
#     container_name: serayu_echo_server
#     working_dir: /app
#     volumes:
#       - ./:/app
#     command: sh -c "npm install -g serayu-echo-server && serayu-echo-server start --config=/app/echo-server.json"
#     ports:
#       - "6001:6001"
#     depends_on:
#       - redis
#     networks:
#       - serayu

networks:
  serayu:
    driver: bridge
